<%= content_tag "div", id: "downvote-#{answer.id}" do %>
<%= link_to downvote_question_answer_path(@question, answer) , method: :patch, remote: true, data: { disable_with: "voting..." } do %>
    <% if person_signed_in? %>
    <% if current_person.voted_down_on? answer %>
      <i class="arrowdo"></i>
    <% else %>
      <i class="arrow2"></i>
    <% end %>
    <% else %>
      <i class="arrow2"></i>
    <% end %>
  <% end %>
<% end %>

